export interface Antelope {
    name: string;
    continent: string;
    weight: number;
    height: number;
    horns: string;
    picture: string;
}

export interface StyledProps {
    className?: string;
}
